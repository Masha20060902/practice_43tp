<Window x:Class="DocumentVersion.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentVersion.ViewModel"
        mc:Ignorable="d"
        Title="Идентификация" 
        Height="330" 
        Width="526" 
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}">
    <Window.DataContext>
        <local:LoginViewModel/>
    </Window.DataContext>
    <Grid Margin="105,45,105,45">
        <Grid.RowDefinitions>
            <RowDefinition Height="57"/>
            <RowDefinition Height="57"/>
            <RowDefinition Height="46"/>
            <RowDefinition Height="44"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Content="Имя" FontWeight="Medium"/>
            <TextBox Text= "{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Height="27" FontSize="15" Style="{DynamicResource AppTextBoxStyle}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <StackPanel>
                <Label Content="Пароль" FontWeight="Medium"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Height="27" FontSize="15" Style="{DynamicResource AppPasswordBoxStyle}"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <Label Content="{Binding StatusMessage}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="248"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Command="{Binding LoginCommand}" Width="116" Height="28" Style="{DynamicResource AppButtonStyle}">
                    <ContentPresenter Content="Вход"/>
                </Button>
                <Button Command="{Binding RegisrterCommand}" Width="116" Height="28" Margin="15,0,0,0" Style="{DynamicResource AppButtonStyle}">
                    <ContentPresenter Content="Регистрация"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
