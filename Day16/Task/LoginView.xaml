<Window x:Class="Task.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task"
        mc:Ignorable="d"
        Title="LoginView" Height="330" Width="526" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <TextBox Text= "{Binding UserName, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" Width="191" Height="30" VerticalAlignment="Top" Margin="0,103,110,0"/>
        <PasswordBox Margin="0,157,110,0" x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" HorizontalAlignment="Right" Width="191" Height="28" VerticalAlignment="Top">
        </PasswordBox>
        <Button Content="Вход" HorizontalAlignment="Right" Margin="0,204,303,0" VerticalAlignment="Top" Height="34" Width="113" Command="{Binding LoginCommand}" Click="Button_Click_1"/>
        <Button Content="Регистрация" HorizontalAlignment="Right" Margin="0,204,110,0" VerticalAlignment="Top" Height="34" Width="154" Command="{Binding RegisrterCommand}" Click="Button_Click"/>
        <TextBlock Text="{Binding StatusMessage}" Margin="0,0,105,250" HorizontalAlignment="Right" Width="196" Height="37" VerticalAlignment="Bottom"/>
        <Label Content="Введите имя" HorizontalAlignment="Right" Margin="0,105,306,0" VerticalAlignment="Top" Width="110"/>
        <Label Content="Введите пароль" HorizontalAlignment="Right" Margin="0,158,304,0" VerticalAlignment="Top" Width="110"/>
        <Label Content="Идентификация" HorizontalAlignment="Right" Margin="0,27,382,0" VerticalAlignment="Top" Width="110"/>
    </Grid>
</Window>
