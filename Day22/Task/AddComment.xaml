<Window x:Class="Task.AddComment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task"
        mc:Ignorable="d"
        Title="Добавление комментария"
        WindowStartupLocation="CenterScreen"
        Height="450" 
        Width="800">
    <Grid Margin="67,0,67,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="285"/>
            <RowDefinition Height="56"/>
        </Grid.RowDefinitions>
        <StackPanel >
            <Label Content="Заголовок" FontWeight="Medium"/>
            <TextBox x:Name="Title" Height="39" FontSize="18">
                <TextBox.Template>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </TextBox.Template>
            </TextBox>
        </StackPanel>
        <Grid Grid.Row="1">
            <StackPanel>
                <Label Content="Комментарий" FontWeight="Medium"/>
                <TextBox x:Name="Comment" Height="256" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="251"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column ="2" Orientation="Horizontal">
                <Button x:Name="Add" Width="116" Height="28" Click="Add_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#D9D9D9" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                <ContentPresenter Content="Добавить" HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="Cancel" Width="116" Height="28" Margin="19,0,0,0" Click="Cancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#D9D9D9" BorderBrush ="Black" BorderThickness="1" CornerRadius="5">
                                <ContentPresenter Content="Отмена" HorizontalAlignment="Center" VerticalAlignment="Center">
                                </ContentPresenter>
                            </Border>

                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
